kind = "web"
previewPath = "/"
title = "Dar Listings"
version = "1.0.0"
id = "artifacts/dar-listings"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20347

[services.development]
run = "pnpm --filter @workspace/dar-listings run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/dar-listings", "run", "build" ]
publicDir = "artifacts/dar-listings/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20347"
BASE_PATH = "/"
